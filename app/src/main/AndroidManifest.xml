<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

<!--    <uses-permission android:name="android.permission.CAMERA" />-->

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher_for_start"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_for_start_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Aston_Intensive2"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>
        <activity android:name=".lesson1.HelloWorldActivity" />
        <activity android:name=".lesson1.InteractiveUIActivity" />
        <activity android:name=".lesson1.LayoutEditorActivity" />
        <activity android:name=".lesson1.ScrollingViewActivity" />
        <activity android:name=".lesson1.HelpYourSelfActivity" />
        <activity android:name=".lesson2.intents.IntentsActivity" />
        <activity android:name=".lesson2.intents.IntentsSecondActivity" />
        <activity android:name=".lesson2.LifecycleStateActivity" />
        <activity android:name=".lesson2.ImplicitIntentsActivity" />
    </application>

</manifest>